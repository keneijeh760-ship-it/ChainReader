/**
 * MQTT messaging integration for robot and vision services.
 *
 * <p>Subpackages:
 * <ul>
 *   <li>{@code topics} — central topic constants</li>
 *   <li>{@code events} — typed event payloads</li>
 *   <li>{@code serialization} — Jackson JSON conversion</li>
 *   <li>{@code config} — broker connection and client gateway</li>
 *   <li>{@code publisher} — outbound robot commands</li>
 *   <li>{@code subscriber} — inbound robot events</li>
 * </ul>
 */
package com.vectoros.fleet.mqtt;
